<Window x:Class="DndHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DndHelper"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!--<Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="{}"/>
                </StackPanel>
            </Grid>
        </DockPanel>-->
        <TabControl Grid.RowSpan="1" Margin="2,0,2,0">
            <TabItem Header="Character">
                <DockPanel>
                    <DockPanel DockPanel.Dock="Left" Margin="5,5,5,5">
                        <TextBox DockPanel.Dock="Top"
                     Text="{Binding SearchCharacterString, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>
                        <StackPanel DockPanel.Dock="Bottom" VerticalAlignment="Bottom" Margin="0,5,0,0">
                            <Button x:Name="AddUser" DockPanel.Dock="Left" Command="{Binding AddUserCommand}">Добавить</Button>
                            <Button x:Name="DeleteUser" DockPanel.Dock="Right" Command="{Binding DeleteUserCommand}">Удалить</Button>
                        </StackPanel>

                        <ListView Name="UserList" 
                      ItemsSource="{Binding FilteredCharacters}" 
                      SelectedItem="{Binding SelectedCharacter}"
                      ScrollViewer.CanContentScroll="True" 
                      ScrollViewer.VerticalScrollBarVisibility="Hidden">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Class" DisplayMemberBinding="{Binding Path=ClassCharacter}"/>
                                    <GridViewColumn Header="Race" DisplayMemberBinding="{Binding Path=Race}"/>
                                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Path=Name}"/>
                                    <GridViewColumn Header="Level" DisplayMemberBinding="{Binding Path=Level}"/>
                                </GridView>
                            </ListView.View>
                        </ListView>

                    </DockPanel>
                    <TabControl>
                        <TabItem Header="Name">
                            <StackPanel DataContext="{Binding SelectedCharacter}" Margin="0,5,0,5">
                                <TextBlock Text="Выбранный Персонаж" Margin="5,0,0,0" />
                                <TextBlock Text="Имя" Margin="5,0,0,0" />
                                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"             Margin="5,5,0,5" />
                                <TextBlock Text="Раса" Margin="5,0,0,0" />
                                <TextBox Text="{Binding Race, UpdateSourceTrigger=PropertyChanged}"             Margin="5,5,0,0" />
                                <TextBlock Text="Класс" Margin="5,0,0,0" />
                                <TextBox Text="{Binding ClassCharacter,                                         UpdateSourceTrigger=PropertyChanged}" Margin="5,5,0,0" />
                                <TextBlock Text="Уровень" Margin="5,0,0,0" />
                                    <TextBox Text="{Binding Level,                           UpdateSourceTrigger=PropertyChanged}" Margin="5,5,0,0" />    
                            </StackPanel>
                        </TabItem>
                        <TabItem Header="States">
                            <ListBox ItemsSource="{Binding SelectedCharacter.States}" Margin="0,10,0,10">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding Value}"/>
                                            <TextBlock Text="{Binding Modifier}"/>

                                            <!-- ListBox для отображения Skills -->
                                            <ListBox ItemsSource="{Binding Skills}" Margin="0,5,0,5">
                                                <ListBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <WrapPanel>
                                                            <TextBlock Text="{Binding Modifier}"/>
                                                            <TextBlock Text="{Binding Proficient}"/>
                                                            <TextBlock Text="{Binding Name}" FontStyle="Italic"/>
                                                        </WrapPanel>
                                                    </DataTemplate>
                                                </ListBox.ItemTemplate>
                                            </ListBox>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </TabItem>
                    </TabControl>
                    

                </DockPanel>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
